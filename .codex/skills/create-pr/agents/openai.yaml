interface:
  display_name: "Create PR"
  short_description: "필요 시 커밋 후 이슈 자동 종료 포함 한국어 PR 생성"
  default_prompt: "Use $create-pr to inspect current branch changes, create needed $git-commit commits, identify related GitHub issues, run full tests, draft a Korean PR, and publish it after approval."

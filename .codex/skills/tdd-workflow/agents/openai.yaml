interface:
  display_name: "TDD Workflow"
  short_description: "이슈 브랜치 준비 후 RED부터 보안 검토와 AC 검증까지 통합 실행"
  default_prompt: "Use $tdd-workflow auth-1 to prepare feature/auth-001 style branch, run RED, GREEN, BLUE, security review, security-fix BLUE rechecks, and AC verification."

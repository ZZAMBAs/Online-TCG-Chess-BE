interface:
  display_name: "TDD Workflow"
  short_description: "TDD 단계 실행과 필요 시 단계별 커밋까지 통합 관리"
  default_prompt: "Use $tdd-workflow auth-1 to prepare the issue branch, run RED, GREEN, BLUE, security review, optional $git-commit checkpoints, and AC verification."
